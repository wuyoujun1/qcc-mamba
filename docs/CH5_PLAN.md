# 第五章改造清单（2026-09-12 起，次日继续）

> 目标文件：`paper/ch5/ch5cn_pdf.py`（第五章定版生成器）。改完重跑生成 PDF + 同步本地 `~/paper/ch5/`。

---

## 0. 状态总览

### ✅ 今天已完成
1. **删** 5.1 的"偏差不超过 2.5%"整句（你定的：不提复现偏差）。
2. **删** 5.1 的"若训练出现显存不足，就把批量减半"（日志里 OOM 命中 0 次）。
3. **删** 5.1 的敏感性残留"测试结果对其取值不敏感（变化小于 1%）"（该节已删，无支撑）。
4. **删** 两个残句："基线复现。"、"实现细节与训练协议。"。
5. `ch5_supp_20260909/partial_removal.py` 的输出路径从写死的 `/home/youjun/paper/ch5_supp_20260909/` 改为脚本自身目录。
6. 实跑屏蔽实验扫描（GPU3，最新 raw_k 代码 + 最新 ckpt），结果存 `ch5_supp_20260909/partial_*.json`（见 §6）。

### ✅ 2026-09-12 下午补做
- **§1 结构改造 A/B/C/D 已完成**：`A 实验配置与主结果`(L117) / `B 消融实验`(L237) / `C 机制验证`(L248) / `D 可解释性`(L263)；机制块已挪到消融块之后，D 节把屏蔽实验挪到热力图之前。定版 PDF 重出（6 页）。
- **§2 表号重排已完成**：表 3（消融组件）/ 表 4（消融数值）/ 表 5（灵敏度）/ 表 6（屏蔽）；表 1、表 2 不变。
- **§3 五段新增文字已插入**（章首总起、B/C 配置段、C 细节段）。
- **§4 部分完成**：斯皮尔曼整段+表已删；5.2"Beijing 八条基线仍在数据工作文件中补全"已删；表 6 措辞改为"相对同一模型未屏蔽时"，"作用分散"结论句已删。
- 遗留：两张旧 K 图（`figs/ch5_k_ETTh1_96.png`、`ch5_k_ETTm2_96.png`）未删；D 节图序仍是 2a→2b→2c→2d→ECDF段（未按 §1 建议调成 2a→2b→2d→ECDF→2c）。

### ⬜ 待办
- §4 余项（两张旧 K 图）
- §5 图 2 配色
- §6 表 7 换方向（决定甲/乙后重跑，数字替换上表）

---

## 1. 结构改造（师姐指示：第五章不再分五节）

| 新 | 标题 | 内容来源 | 表图 |
|---|---|---|---|
| — | 第五章　实验 | — | — |
| — | **〔新增〕章首总起 2 句** | 见 §3① | — |
| **A** | 实验配置与主结果 | 原 5.1 两段（配置）+ 原 5.2 两段（主结果）合并 | 表 1、表 2、图 1 |
| **B** | 消融实验 | **新写配置段**（§3②）+ 原 5.4 那段 | 表 3 ← 原表4、表 4 ← 原表5 |
| **C** | 机制验证 | **新写配置段**（§3③）+ **新写细节段**（§3④）+ 原 5.3 结论句 | 表 5 ← 原表3 |
| **D** | 可解释性 | 屏蔽段（前移，措辞见 §6）+ 热力图段 + ECDF 段；**删斯皮尔曼整段** | 表 6 ← 原表7、图 2a/2b/2d/2c |

**D 节内部顺序**（按师姐"实验放前面，再放热力图"）：
屏蔽实验段 → 表 6 → 热力图解读段 → 图 2a → 图 2b → 图 2d → ECDF 段 → 图 2c

> D 节默认**不加配置段**（师姐只对 a/b/c 说了"先写配置"）。若也要，加一句即可。

---

## 2. 表图编号映射（连带要改的行内引用）

| 位置 | 现在 | 改成 |
|---|---|---|
| `:237` 正文 | 表 3 在两个数据集上给出同一现象 | 表 5 … |
| `:238` caption | 表 3　…灵敏度 | **表 5**　… |
| `:247` 正文 | 表 5 在五个耦合数据集上…；表 4 汇总…，表 5 给出数值结果 | **表 3** 汇总…，**表 4** 给出数值… |
| `:248` caption | 表 4　各消融变体的组件构成 | **表 3**　… |
| `:254` caption | 表 5　消融数值结果 | **表 4**　… |
| `:272` 正文 | 表 7 在多个数据集… | **表 6** … |
| `:273` caption | 表 7　按比例屏蔽… | **表 6**　… |
| `:268–271` | 斯皮尔曼段 + 表 6 | **整段删除** |

表 1、表 2、图 1、图 2a–2d 编号不变。

---

## 3. 新增文字（逐字草稿）

**① 章首总起**（接"第五章　实验"之后）
> 本章在九个多变量时序数据集上验证 QCCK-M。A 节给出实验配置与主结果，B 节用消融分离量子核与频域监督两组件的贡献，C 节用输入扰动实验检验主干是否真正利用了跨变量信息，D 节给出耦合矩阵的可解释性结果。

**② B 节配置段**
> 消融实验的配置与 A 相同，仅在数据范围与模型变体上不同：消融在 ETTh1、ETTh2、ETTm1、ETTm2 与 Weather 五个数据集上进行，对每个变体分别替换或去掉表 3 所列的组件，主干配置、训练协议与评测口径均与 A 一致。

**③ C 节配置段**
> 机制验证沿用 A 的模型，不重新训练，仅在评测方式上不同：固定 ETTh1 与 ETTm2 的 96 步设置，取测试集的前八个样本，对输入施加扰动并比较扰动前后的预测。

**④ C 节细节段**（补 5.3 的"怎么加扰动、响应怎么算"）
> 具体做法是：对第 j 个输入变量整体施加一个扰动，扰动幅度取该变量在样本内的时序标准差的 0.1 倍，正负号随机，每个变量重复三次以抵消随机性；记扰动前后的预测为 ŷ 与 ŷ′，则输出通道 i 对输入变量 j 的相对响应定义为 S[i,j] = ‖ŷ′_i − ŷ_i‖ / ‖ŷ_i‖，即该通道预测序列的变化幅度相对于自身预测幅度的比例，除以自身范数是为了让不同量级的变量可比。把 S 的对角元素平均得到对角响应，代表变量对自身的影响；把非对角元素平均得到非对角响应，代表变量对其它变量的影响；两者之比即跨变量影响相对于自身影响的强度。

> 依据：原始方法脚本 `/tmp/sensitivity_analysis.py`（ε=0.1σ、正负随机、重复 3 次、8 个测试样本）；模型 `bsl_ETTh1_96_s2024` / `qf2_ETTh1_96_q_base_s2024`，**与表 2 是同一批模型** ✓

---

## 4. 删除清单（除 §0 已删的）

1. **斯皮尔曼**：5.5 那段"学习到的耦合排序与数据一致"整段 + 表 6 + `results/spearman_results.csv` 的正文引用。
2. **5.2 末尾**："Beijing 上八条基线的结果仍在数据工作文件中补全" —— 自曝未完成，且 `BJMMA` 里八条基线数据其实已齐。
3. **`figs/` 里两张遗留 K 图**：`ch5_k_ETTh1_96.png`、`ch5_k_ETTm2_96.png`（340×300 旧版；生成器用的是不带 `_96` 的那对，已核 md5 与 `ch5_supp/figs/K_*` 的正确版一致）。

---

## 5. 图 2 配色（按 `/home/youjun/画图配色.md`）

| 图 | 类型 | 用哪套 |
|---|---|---|
| 图 2a/2b/2d（K 热力图） | 全正、顺序量 | **方案二红端顺序色**：低 `#F2F2F2` → 中 `#F6A69A` → 高 `#F0766A`；对角掩码 `#D9E2EB` |
| 图 1（winheat，MSE 降低%，有正负） | 发散量 | **方案二发散**：正→`#F0766A`、0→`#F2F2F2`、负→`#1F8FAE` |
| 图 2c（ECDF 折线） | 折线 | **方案一四色** `#FFC6BC / #5FA3CB / #668FCA / #C9CEFE` |

> 配色文档写的是"红-白-青蓝"**发散**色；K 矩阵取值 0–0.6 全正，中间放白会被误读成"0.5 是中性"，**所以只取红端做顺序色**。图 1 才是正负量，用完整发散。

**要重跑**：3 张 K 图 + 图 1。`gen_figs_stats.py` 里 `SRC = "/home/youjun/paper/figs"` 已因本地目录重排失效，改为相对脚本路径（顺带清掉 `docs/TOFIX.md` 里"造图脚本写死路径"那条）。

---

## 6. 表 7（屏蔽实验）：**决定换方向**，以下为今日实测证据

### 6.1 实跑结果（GPU3，最新 raw_k 代码 + 最新 ckpt；`Δ` 为测试 MSE 相对同一模型未屏蔽时的变化）

| 模型 | 50% | 75% | 100% |
|---|---|---|---|
| `bal_ETTh1_96_raw_k_g0.1`（γ=0.1） | — | +2.7%（旧表） | **+0.80%** |
| `conf_Beijing_96_g10`（γ=1.0） | +3.44% | +58.96% | **+108.37%** |
| `bal_Weather_96_raw_k_g1.0`（γ=1.0） | +6.04% | +22.36% | **+112.53%** |

明细 JSON 在 `paper/ch5/ch5_supp_20260909/partial_*.json`（含 `top_delta` / `bot_delta` / `rand_delta` 三个对照）。

### 6.2 结论：现在的表 7 测的不是"去掉耦合"

- **理论**：raw_k 下 `H_p = K_off·H_q`，全屏蔽应让该层**退化为恒等**（`H' = H`）→ 整网 = **A1（S-Mamba + FreDF）**，比 S-Mamba 好。所以**最差不会与 S-Mamba 持平，下界是 A1**（ETTh1-96：A1 0.3757 vs S-Mamba 0.3877）。
- **实际**：γ=0.1 时全屏蔽只 +0.80%（≈ A1 ✓ 符合理论）；**γ=1.0 时全屏蔽 +108%**。
- **原因**（`qcc/quantum_mix.py`，raw_k 分支之后）：
  ```python
  _h   = self.ln(Hp)        # self.ln = nn.LayerNorm(d_token)
  Hp_g = self.hp_scale * _h
  return H + γ * Hp_g
  ```
  `K_off` 全零 → `Hp = 0` → **`LayerNorm(0) = β`（可学习偏置，不是 0）** → 每层被加上 `γ·β` 这个常向量；γ=1.0、两层叠加 → 表示被推离 → 误差翻倍。**这是实现假象，不是"耦合被去掉"。**
- **`RENORM` 是死的**：raw_k 分支（`:525`）**没有重归一化代码**（只有 softmax 分支有），所以 `RENORM=0/1` 跑出的数一字不差。
- **排序本身有效**：75% 时 移除最强 +58.96% vs **随机 +8.36%** vs 移除最弱 +14.90%（差 7 倍）；但 50% 时反过来（最强 +3.44% < 最弱 +4.37%），不稳。

### 6.3 决定：**整个换掉**（用户 2026-09-13 定）。方案如下。

**新实验：耦合强度 ↔ 量子核增益（散点图，新图 3）**

**要立的命题**：QCCK-M 的增益来自显式耦合建模——数据集自身耦合越强/越密，量子核能拿到的东西越多、增益越大；耦合弱则增益趋零。这条把"可解释性"接到"性能"上：耦合矩阵 K 不只是可视化产物，它**预测了方法何时有效**。

**图**（每个点 = 一个数据集，共 9 个）
- 横轴 x：数据集耦合度，从 `f` 的非对角元算，三选一
  - x₁ = 非对角 `f` 的均值
  - x₂ = 非对角 `f` 的中位数
  - x₃ = 强对占比（`f > 0.1` 的变量对比例）—— **与图 2c 的 ECDF 同口径，首选**
- 纵轴 y：该数据集上 QCCK-M 相对 S-Mamba 的 MSE 降低百分比（主表口径；取 4 个预测长度的均值，或 @96 为主、@720 画空心点）
- 点大小 ∝ 变量数（把"大变量弱耦合"那批视觉上分出来）
- 拟合线 + ρ 可以给，但**只有 9 个点，只作趋势示意、不声称显著性**

**预计要干的活**
1. **补抽 3 个数据集的平均 `f`**：现有 `figs/f_*.npz` 只有 7 个（ETTh1/ETTh2/ETTm1/ETTm2/ECL/Traffic/ETTh1_720），**缺 Weather、Exchange、Beijing**。口径照旧（测试集前若干 batch 逐样本 f 求平均）→ 推理，一张卡几分钟。
2. 算 9 个数据集的 x₁/x₂/x₃。
3. 画散点（图 3），配色用最终定下的那套。
4. 正文段落待起草：x/y 各是什么、怎么算、看到什么趋势、解释了什麼。

**预期与风险**
- 预期：ETTh1/ETTh2/Beijing 落右上（耦合强、增益大），ECL/Traffic 落左下（耦合弱、增益小）——与正文 5.2/5.5 已有说法一致。
- 风险：9 个点里若 ETTm1/ETTm2/Exchange 落点不规律，趋势会散。**先画出来看**，不行再换 x 的定义（x₁/x₂/x₃ 三选一）。

> 备注：生成器里的 `COUP`（9 个数据集 + 变量数 + 一个 0.24~0.64 的数）是**死代码**，从没被引用过，第三列来源不明——新实验不要直接用它，横轴一律从 `f` 现算。

---

## 7. 已定、不要改回去

- **理论口径**：两层都纠缠（n=2）；代码暂不改（见 `docs/TOFIX.md`）。
- **表 2 的 Exchange 720 / Traffic 336** 按"与 S-Mamba 基本持平"报。
- **5.1 那句"实验在 8 张 RTX 4090 上完成"不改**（你说 8 卡没做串行、改不改没区别）。
- **5.5"主要强耦合变量对在不同预测长度下保持一致"保持原文，不弱化**（你说 K96/K720 那个 p=0.27 没有参考价值，不作依据）。
- 5.4 的"五个**耦合**数据集"这个说法待定：消融实际覆盖 ETT×4 + Weather，而 Weather 在正文里属弱耦合（要不要改成"五个数据集"由你定）。

---

## 8. 次日开工顺序建议

1. 定表 7 走方向甲还是乙（§6.3）
2. 改结构 A/B/C/D + 表号重排（§1、§2）—— 一次改完再重出 PDF
3. 插入 §3 的五段文字
4. 执行 §4 删除清单
5. 重造图（§5）→ 重出 PDF → 同步 `~/paper/ch5/` → 推 GitHub
