# QCC-Mamba 实验架构说明（Method 章，不含结果）

> 目的：讲清最终收敛的实验架构长什么样、每个组件为什么存在、怎么组合。
> 配合阅读：`EXPERIMENT_CHAPTER.md`（实验结果）、`BACKBONE_CONFIG.md`（逐 flag 公式）、`baseline_local.md`（基线）。

---

## 0. 一句话

**QCC-Mamba = 官方 S-Mamba 主干（零改动）+ 每层后插一个量子混合层（QuantumMixLayer）+ FreDF 频域监督，主干超参与官方复现配置对齐，按数据集特化。**

---

## 1. 整体架构

```
输入 H (B, L, V)  ──►  S-Mamba Encoder Layer 1 ──►  QuantumMixLayer 1 ──►
                      S-Mamba Encoder Layer 2 ──►  QuantumMixLayer 2 ──►  Decoder ──► 预测
```

- **主干**：官方 `S_Mamba`（零结构改动）。只靠超参对齐官方复现配置。
- **量子混合层**：插在前 2 个 Encoder 层之后（`--qmix_layers 2`），输出维数不变。

### 量子混合层内部（`qcc/quantum_mix.py`）

```
H' = LN( H + gate · LN( Hp ) )        # 残差 + 门控混合
Hp = K_n · ( H · W_q )                # 跨变量消息传递
K  = softmax( (K_raw − I) / kernel_T ) # 去对角 + 温度缩放的行归一化邻接
```

- `W_q`：跨变量消息映射（可学习）。
- `K`：(B, V, V) 变量间耦合权重，softmax 行归一化，`Hp` 幅度与 H 同量级（不被 1/V 稀释）。
- `gate`：可学习标量门（`gate_init=0.1`），控制量子混合强度。`gate≈0` 时量子层≈恒等。

---

## 2. 量子核（核心差异化组件）

### 2.1 编码：纠缠特征映射
- 输入 H 经纠缠 feature map（含 CNOT）编码为量子态 ψ，形状 **(B, V, 2^N)** 复向量。
- **`n_qubits=5`** → 32 维复 Hilbert 空间（`--n_qubits 5`）。
- S-only 路径（`--qmix_use_S_only`）：量子核**直接读对齐频谱 S** 编码，S 是确定性函数（内部全 detach），让量子核拿到主干拿不到的频域结构。

### 2.2 核函数：`quantum_exp`（测地核，带宽可学习）
```
K_raw[i,j] = 量子态保真度/测地距离函数(ψ_i, ψ_j)，带宽 κ 可学习（kernel_exp_learn）
```
- 与 rbf 的固定 γ 不同，**κ 随训练自适应**。
- **`--offdiag`**：K 去对角再 softmax（保真度核 diag=1 会使 softmax 坍缩到恒等）。

### 2.3 温度：`kernel_T`（视界自适应旋钮）
- `kernel_T` 放大核差异：96/192/336 用 **0.1**，720 用 **0.2**（ETTm1:720 试 0.3）。
- 这是量子核的"专用调参旋钮"，rbf 没有对应物。

### 2.4 经典核公平对照（消融用）
- `--kernel_fn rbf --n_qubits 5`：rbf 作用于**同一个** 2^5 维量子编码特征（复数拼接 [Re; Im] 算 cdist），与量子核完全同配置同 seed。

---

## 3. FreDF 频域监督（超基线主力，纯训练项）

```
L = l_t + λ · |rfft(pred) − rfft(true)|.mean()    # λ=1.0, lowpass=1.0, complex 模式
```
- 频域监督在时域 MSE 之外加入**频域幅度对齐**，稳定长时序预测。
- 与官方 Master-PLC/FreDF 逐行一致（已复现验证）。
- **这是"稳定超官方基线"的主要机制**。量子核是补充。

---

## 4. 门控机制（微架构适配）

### 4.1 全局门（默认）
- `--qmix_gate --qmix_gate_init 0.1`：可学习标量门，温和渐进开 gate。

### 4.2 每变量门控（弱耦合/大变量数据集）
```
--qmix_gate_pv        # 每目标变量：是否接受跨变量消息
--qmix_gate_pv_src    # 每源变量：可学习 topk（K 列选择性），init -3 默认关
```
- 缓解数百独立表计（ECL/traffic）下 K 退化为全局平均的"量子税"。
- **gclose（三路全关门控）**：`gate_init 0.0 + pv_init -6 + pv_src_init -6`，把量子注入减到最小。

---

## 5. 频域对齐（数据集相关）

```
--align_lambda 0.1 --align_mode freq --align_norm 1
```
- 目标跨谱相干（CSD 归一化），让量子核读的对齐频谱更准。
- **按数据集启用**：ETTm2 大赢；ETTh1:96 微伤 → 不是全用。

---

## 6. 主干超参（官方复现对齐）

| 数据集 | d_model | d_state | e_layers | lr | ep | batch |
|---|---|---|---|---|---|---|
| ETTh1 | 256 | 2 | 2 | 7e-5 | 10 | 32 |
| ETTh2 | 256 | 2 | 2 | 4e-5 | 10 | 32 |
| ETTm1 | 256(96)/128 | 2 | 2 | 5e-5 | 10 | 32 |
| ETTm2 | 256(≤192)/128 | 2 | 2 | 5e-5 | 10 | 32 |
| weather | 512 | 2 | 3 | 5e-5 | 5 | 32 |
| ECL | 512 | 16 | 3 | 1e-3 | 5 | 16 |
| traffic | 512 | 32 | 4 | 1e-3 | 10 | 16 |
| Exchange | 128 | 32 | 2 | 1e-4 | 10 | 32 |

> 量子核在**主干上叠加**，不改这些超参（除少数格 lr/kernel_T 特化）。

---

## 7. 视界自适应旋钮（每格选配）

| horizon | kernel_T | train_epochs | learning_rate |
|---|---|---|---|
| 96 / 192 / 336 | 0.1 | 10 | 1.5e-4（官方对齐格用官方 lr） |
| 720 | 0.2 | 20 | 2e-4 |

---

## 8. 数据集特化汇总

| 数据集 | 特化 |
|---|---|
| **ETTh1/ETTh2/ETTm1** | 标准量子核公式，主干对齐官方 |
| **ETTm2** | + freq-align（频域对齐） |
| **weather** | 弱耦合，量子核增量小，主干对齐（d512） |
| **ECL** | d512 + 门控减税（gclose），高 lr |
| **traffic** | d512 + 门控，弱耦合税大，尽力不输 |
| **Exchange** | d128 + 无 FreDF（非周期，FreDF 有害）+ 门控 |

---

## 9. 消融设计（验证各组件贡献）

| 模型 | 量子核 | FreDF | 说明 |
|---|---|---|---|
| **Full** | ✓ (nq5) | ✓ | 完整 QCC-Mamba |
| **A1** | ✗ | ✓ | S-Mamba + FreDF（去量子层）→ FreDF 贡献 |
| **A2** | rbf@nq5 | ✓ | 经典核公平对照 → 量子核 vs rbf |
| **A3** | ✓ (nq5) | ✗ | 量子核无频域监督 → FreDF 贡献 |
| **A4** | rbf@nq5 | ✗ | 纯经典核无监督 → 底线 |

---

## 10. 种子与对比口径

- **基线**：官方 S-Mamba 复现（seed 2023），8 数据集全 horizon。
- **模型**：s2023 主 + 第二 seed（s2024/2025），最终口径由用户定（2 平均 or 3 平均）。
- **消融**：同种子同配置，只切组件。

---

## 11. 为什么这样设计（决策链）

1. **量子核的价值 = 维度杠杆 + 耦合结构编码**：nq5 的 2^5 特征空间 + 纠缠编码，在**强/中耦合**数据集（ETT 系、weather）提供经典核难显式构造的结构。
2. **kernel_T 是量子核专属旋钮**：长 horizon（720）温度要调高，是 720 翻盘关键。
3. **FreDF 是超基线主力**：频域监督稳定，消融 A4→A1 差 3-6%。
4. **rbf 公平对照必须 nq5**：量子核 5 bit，rbf 也该 5 bit（作用同一编码特征），否则不公平。
5. **非耦合大变量（ECL/traffic）量子是税**：门控减税但结构上难赢，论文如实标注。

---

*文档待补充：最终各格收敛配置表（等 3-seed 跑完）、收敛曲线、组件贡献量化。*
