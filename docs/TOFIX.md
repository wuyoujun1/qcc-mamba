# 待解决问题清单

> 2026-09-11 整理。**按优先级排，每条都给了证据与解法**。解决后在下面划掉并注明日期。

---

## ✅ 已决定（不用再动）

### 表 2 的 Exchange 720 / Traffic 336 按"持平"报
- **2026-09-11 用户拍板：这两格就是按"与 S-Mamba 基本持平"报，定版数字为准。** 不需要改成"略低"。
- 附记（备查，不必处理）：第五章定版 `ch5cn_pdf.py` 的 `OFF_SM`（表 2 S-Mamba 列）与 `data/第五章_主表消融_MSEMAE_20260904.md` 主表段有 17/30 格不同，其中上面这两格 md 填的是 QCC 自己的值（0.8595 / 0.4165）。**以定版为准**；md 若日后要对齐，回 `dataops_ws/logs/` 重导 S-Mamba 复现值即可（低优先）。

---

## P1 — 论文正文与代码有 6 处对不上（清单见 README）

见 `README.md` 的「⚠️ 代码口径差异」第二节。摘要：

| 正文写法 | 代码实际 |
|---|---|
| 编码态经**两层相同的 U**演化 `U_v·U_v`（两层各含 CNOT） | 首层只旋转不纠缠，纠缠层数 = D−1，即 `D=2` 只有 **1 个** CNOT 层，且两层角度来源不同（H / S） |
| 振幅通道 `p(h_v)=softmax(W_r·h_v)` | 代码无 softmax 编码：`proj_H/proj_S` 是 `Linear(·→2N)`，输出直接当每比特 (θ,φ) 旋转角 |
| 投影矩阵/旋转角**逐变量独立可学习** | `proj_H`/`proj_S`/`W_q` 全变量**共享** |
| `H′ = LN(H + γ·H_p)` | 代码是 `H + γ·LN(H_p)`：有内层 LN、无外层 LN，位置相反 |
| 3.2 主频 `argmax_{f≥1}` 只忽略直流 | 实际跳过 k=0 **和 k=1** |
| `S_v` 恒 2M | 带 `--delay_in_s` 时 **2M+1**（脚本普遍开着） |
| 3.2 两级对齐是必备环节 | `--spectrum_time_align` / `--spectrum_freq_align` 默认**关** |

**怎么解决**：逐条决定「改代码」还是「改正文」。第三章已定稿，涉及第三章的几条基本意味着要改代码（或确认代码偏离是有意的）。

---

## P1 — 造图脚本输出路径写死

`paper/ch5/ch5_winheat.py`、`paper/ch5/ch5_supp_20260909/gen_pred_adv.py` 等仍写死输出到 `/home/youjun/paper/figs/`（不在仓库内）。换机即失败。
**怎么解决**：改成相对脚本的 `paper/ch5/figs/`（可加 `QCC_FIG_DIR` 覆盖）。低风险，随时可做。

---

## P2 — 仓库代码 ≠ 跑出论文结果的代码（**已决定保持现状**）

跑结果的是工作副本 `/home/youjun/dataops_ws/`，仓库 `main` 只是参考快照，缺 `--qmix_norm raw_k` / `rawk_row` / `--qmix_msg` / `--qmix_ln_hp` / `intervention_mask`。
2026-09-11 用户决定：**不同步代码**，只在 README 写明不一致。
**备忘**：照本仓库 clone 跑不出论文任何一格；换机复现需要把工作副本一起带走。

---

## P2 — 未入库的原始数据

`paper/ch5/ch5_supp_20260909/raw_runs/`（150 个逐 run json/npz）只在本地，仓库只给了 `summary_coupling.csv`。若日后要复核斯皮尔曼/屏蔽结果，需要这批原始文件。
