# 量子混合主干重构实验复盘(2026-08-12)

> 从旁路修正到量子核进主干的完整演进、理由、结果与推断严谨性。

---

## 一、为什么从"旁路"改到"主干"

### 1.1 原始架构(DualAE-QCC 旁路)

```
backbone → H (B,V,d) + y_main
S(频谱, detach) → QCCBlock 旁路 → correction
y = y_main + α·correction, loss = MSE(y) + β·MSE(correction, residual)
```

P1F 全矩阵结果:**dual ≈ h_only ≈ baseline**(千分位差异)——量子旁路对预测没有可测贡献。

### 1.2 探针诊断定位死因(analyze_bypass.py)

| 指标 | 实测 | 含义 |
|---|---|---|
| S→0 消融(修正量变化) | 0.02–6%(s_only 对照 23%) | S 信号几乎没有进入修正 |
| Pearson(corr, residual) | 0.02–0.11 | 修正不指向残差 |
| α 终值 | 冻结在初值 | 修正无内容 → α 无梯度 |
| **主干跨变量灵敏度** | **1e-6** | **主干预测逐变量独立** |

两个根因:
1. **S 信号死在残差结构**:Hp 幅度仅 H 的 1.5%,`LN(H + Hp)` 后 S 调制被稀释到 0.02%
2. **主干根本没有跨变量建模**:Mamba 沿 V 维的混合被训练成 ≈0(etth1/electricity 两数据集实测,非对角/对角 = 2–3×10⁻⁶)

### 1.3 关键前提检查:跨变量信息到底有没有价值?

残差结构诊断(etth1 96 plain 模型):

| 指标 | 实测 |
|---|---|
| 残差_v vs 残差_u | 均值 0.50,最大 0.99,95% 变量对 >0.1 |
| 残差_v vs 输入_u(u≠v) | \|r\| 均值 0.765(高于自身输入 0.50) |
| **其他变量可解释的残差方差** | **59.3%(最大 71.7%)** |

**结论:预测误差的一大半是"看一眼其他变量的输入就能修掉的"——跨变量机制有巨大的真实目标,不是伪命题。**

### 1.4 重构决策

- 目标存在(残差 59% 跨变量可解释)、工具存在(K 与真实跨变量相关 Spearman 0.4–0.6)、
  但两次运输(旁路修正、Hp 通道)都失败
- **决策:抛弃旁路,量子核进主干**——K 作为主干的跨变量混合算子,proj_H/proj_S 端到端训练,
  预测从混合后的表征直接出

---

## 二、主干后的四轮尝试

### 轮 1:量子混合进主干(基准确立)

变体:qmix(`H'=LN(H+(1/V)K·H·W_q)`)、qmix_sin(+频谱注入 `X₀+=proj(S)`)、plain(基准)

| cell | plain | qmix | qmix_sin | 赢家 |
|---|---|---|---|---|
| etth1:96 | 0.6124 | 0.6200 | **0.5752** | qmix_sin(−0.037) |
| etth1:720 | 0.8837 | 0.9326 | **0.8700** | qmix_sin(−0.014) |
| etth1:192/336 | — | — | qmix_sin 大幅变差(+0.04/+0.07) | plain |
| weather 全档 | — | — | 全部输 | baseline/plain |

结论:
- **qmix ≈ plain**:量子混合本身无贡献;qmix_sin 的赢全来自频谱注入(不是量子核)
- 频谱注入在 etth1 **两头有益(96/720)、中间有害(192/336)**——疑似频谱采样与档位错配
- 灵敏度全部 1e-6

### 轮 2:运输修复(方案 1 + 方案 2)

理由:1/V 缩放 + 残差 LN 把 Hp 的跨变量信号稀释(幅度仅 H 的 1.5%)
- 方案 2(qmix_soft):消息传递改行 softmax(GAT 式),Hp 幅度 ≈ H
- 方案 1(qmix_head):预测头读 `[H, LN(softmax(K)·H·W_q)]`,K 最接近损失
- qmix_full:1+2+注入

结果:qmix_soft 赢 weather:96(0.1956 < plain 0.1993,首次);无系统性胜出;**灵敏度仍 1e-6**

新诊断(决定性):
- K 与残差同步结构有对齐(Spearman 0.39–0.59)——方向部分正确
- **softmax(K) 行熵 = 1.851 vs 均匀 1.946**——K 近均匀分布!
- 根因:**浓度定理**:2⁸=256 维随机量子态重叠 ≈ 1/256 ≈ 0.004,softmax 把 [0.004+ε] 的差异抹平成均匀 → 消息传递退化为全局平均 → 无变量选择性

### 轮 3(进行中):核选择性修复

理由:浓度定理的直接对策,正交开关设计
- qmix_T10:`softmax(K/0.1)` 温度缩放,放大 0.004 级差异
- qmix_T10_n4:n_qubits=4(2⁴=16 维,重叠 1/16,差异天然变大)+ 温度
- qmix_head_T10:预测头聚合 + 温度
- qmix_topk:softmax 后保留 top-2 重归一化(变量选择)

初步信号:
- **n_qubits=4 的 K 耦合放大 8 倍**(k_off 0.08–0.15 vs 8 比特 0.01–0.03)——浓度定理修法在机制层面生效
- qmix_T10 weather:96 = 0.1988 < plain 0.1993(延续 win)
- MSE 尚未见系统性优势(12-cell 未跑完)

---

## 三、实验得出的信息与严谨性评估

### 可靠结论(多轮一致,机制层)

1. 主干预测对跨变量输入零响应(1e-6)——etth1/electricity 多模型一致测量
2. 残差 59% 可被其他变量线性解释——跨变量目标真实存在
3. 量子核 K 能捕捉真实跨变量结构(Spearman 0.4–0.6)——信息在 K 里
4. 保真度核选择性被高维态浓度抹平(行熵 1.85/1.95)——运输失败的根本原因
5. 频谱注入在 etth1 96/720 有真实收益(2-seed 稳定,−0.037/−0.014)
6. 旁路修正机制整体失效(修正量 0.03–0.6% 残差,相关≈0)

### 严谨性注意(不严谨之处)

1. **轮 3 是 1-seed 实验**——结论需 2-seed 确认。随机性本身已被证明可以很大(pems_bay 96 曾有 70% seed 差异)
2. **对比口径不完全统一**:前期 vs 历史三臂(baseline/h_only/s_only,旧代码旧配置),轮 2 起固定 vs plain——两个口径的结论不能混用
3. **数据残缺**:288 格完整矩阵只跑了约 1/3;轮 3 的 12 cell 尚未跑完
4. **"赢"的稳健性**:etth1 96 赢 0.037 是否超出 seed 噪声,需 2–3 seed 才能定论
5. 一个已确认的口径问题:新代码 plain 在 etth1 720(0.8837)比旧代码 baseline(0.9325)好 0.049——历史数字不可直接对比

### 推断链的严谨性总结

**机制链是严谨的**:每一步都有直接测量(K 结构→softmax 均匀→灵敏度 1e-6),不是推测。
**结论链是不完整的**:机制修好了不代表 MSE 赢,而"哪个配置能赢"的结论远未定论——这正是轮 3 要回答的。

---

## 四、下一步打算与变体选择理由

### 为什么跑这四个变体(轮 3)

| 变体 | 理由(理论依据) |
|---|---|
| qmix_T10 | 浓度定理的直接解药:softmax(K/T),T<1 把 0.004 级差异放大 10 倍。一行改动,成本最低 |
| qmix_T10_n4 | 独立验证"浓度就是元凶":2⁴=16 维态空间,重叠 1/16,差异天然变大;同时省一半量子计算 |
| qmix_head_T10 | 正交维度:核的位置(预测头,最接近损失)与选择性(温度)组合,验证"位置×选择性"交互 |
| qmix_topk | 正交维度:把加权平均变成明确"变量选择"(top-2),与温度是两种不同的选择性机制 |

**判据固定**:① 跨变量灵敏度(1e-6 → 跳升?)= 机制是否打通;② ΔMSE vs plain(≥4/12 cell 赢)= 是否兑现。

### 后续分支(自主决策规则)

- **A. 灵敏度跳升 + 赢 plain**:锁定方向 → 细化(kernel_T ∈ {0.03, 0.01}、topk ∈ {1,3}、+频谱注入组合)→ 2-seed 验证 → 扩展全矩阵
- **B. 灵敏度跳升但 MSE 未赢**:信息到达但不可用 → 检查 K 与残差结构对齐度(0.4 → 提升?)、预测头容量
- **C. 灵敏度仍未跳**:核形式问题 → 最后一搏(去平方核 / 线性实内积核 / 经典核对照)

### 宏观判断

即使量子核最终没有兑现 MSE,可交付成果依然成立:
1. **频谱注入 + 架构改进**(etth1 96/720 真实收益,2-seed 稳定)
2. **完整的机制诊断链**(为什么旁路失败、为什么 K 无选择性——有直接测量支撑)
3. 量子核如实报告为"有结构但未转化为收益"的科学负结果

在"量子核雷打不动"的约束下,先穷尽选择性修复(轮 3),再按分支 A/B/C 推进。
