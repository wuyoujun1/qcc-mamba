# DualAE-QCC 项目 Idea 概览

> 最后更新：2026-08-07
> 状态：DualAE-QCC 重构完成，准备跑实验
> 投稿目标：IEEE TKDE (CCF-A)

---

## 1. 一句话定位

在 S-Mamba 主干上并联**时频双阶段对齐量子编码旁路（DualAE-QCC）**：首层用 backbone 语义特征 H 编码"变量身份"，数据重上传层用**三层对齐后的频谱特征 S** 做"调制"，保真度核度量跨变量相似度作为消息传递权重。

**理论身份**：对时移群/尺度群的商空间表示——把 RevIN 从统计非平稳推广到结构非平稳。

## 2. 核心问题

电力 / 交通 / 能源时序数据有**大量变量**（ECL 321 维、Traffic 862 维），传统 S-Mamba 的 DataEmbeddingInverted 把每个变量当 token 通过隐状态交流，但**缺少显式的跨变量非线性相似度建模**。

| 方法 | 跨变量机制 | 问题 |
|---|---|---|
| iTransformer | Inverted Attention | 二次方复杂度，长序列 OOM |
| TimesNet | 2D 时频变换 | 不直接建模变量间相似度 |
| **DualAE-QCC（本文）** | **时频对齐 + 量子核** | **显式核矩阵 + 商空间不变性 + L 无关** |

## 3. 架构：DualAE-QCC 端到端

```
x (B, L, V)                                    L=输入长度，V=变量数
  → RevIN → S-Mamba Backbone → y_main + H (B,V,d)   d=512
  → 【频谱模块】rfft → 三层对齐 → 重采样 M 点 → S (B,V,2M)   [全 detach]
  → 【量子旁路，最终方案】
       首层：|ψ⟩ = ⊗ RZ(πφ_Hi)RY(πθ_Hi)|0⟩        θ_H=Linear_H(d→2N)，N=10
       重上传：|ψ⟩ = U_ent·(⊗ RZ(πφ_Si)RY(πθ_Si))·|ψ⟩ ×(D−1)  θ_S=Linear_S(2M→2N)
       → K[i,j]=|⟨ψ_i|ψ_j⟩|² → H'=(1/V)K(W_q H) → y = y_main + α·proj(H+H')
  → RevIN denorm
```

**关键设计**：
- **H 只进首层**（语义身份），**S 每层重上传**（频谱调制）——"身份固定 + 调制变化"
- 酉门复合不覆盖信息：R(θ_S)·R(θ_H)|0⟩ 是复合旋转，最终态同时依赖 H 和 S
- qubit 主线 **N=10**（2¹⁰=1024，正好 2 倍于 512 输入），9/8/12 消融

详见 [IDEA_DualAE_QCC.md](file:///d:/download/qutest/qcc_mamba/IDEA_DualAE_QCC.md)。

## 4. 频谱特征构造：三层对齐（全 detach 确定性函数）

### 4.1 频谱分解
```
X(k) = Σ_{n=0}^{L-1} x(n)·e^{-i2πkn/L}     k=0..L−1     DFT
rfft: L=720 → 361 个复数值（实信号共轭对称，只留一半+1）
A(k)=|X(k)|   幅度谱        φ(k)=arg X(k)   相位谱
```

### 4.2 第 1 层：时间轴对齐（相位去趋势，去时移基准）
```
时移定理：x(t−δ) ⟷ X(f)·e^{-i2πfδ}      → 相位谱多一条斜率 2πδ 的直线
δ̂_v = 变量 v 相对"共识时钟"的时移（样本内留一均值归一化互相关最优滞后）
φ̃(f) = φ(f) − 2πf·δ̂_v                    （每个频率减自己的偏移量）
```

### 4.3 第 2 层：频率轴对齐（主频归一化，去周期刻度差异）
```
尺度定理：x(αt) ⟷ (1/|α|)·X(f/α)
f̂_peak = argmax A(f)（幅度谱主峰）
f̃ = f / f̂_peak                          → 主峰搬到 f̃=1.0
采样区间 [0,2]（主线）/[0,1]（消融）
重采样：在 f̃ 轴上取 M=32 个均匀点 → 线性插值
```

### 4.4 第 3 层：幅度归一化（消融档，主线不做）
```
Ã = A / A_max        （每条变量独立）
```

### 4.5 输出
```
S_v = [Ã(32 幅度) ; φ̃(32 相位)] ∈ R^64    频率轴隐含在索引顺序里
```

## 5. 量子核工作流（4 步）

```
┌────────────────────────────────────────────────────────────┐
│ Step 1  首层编码（H → 角度）                                 │
│   H_norm = LayerNorm(H)                                    │
│   θ_H, φ_H = proj_H(H_norm)  → (B, V, N, 2)              │
│   |ψ⟩ = ⊗_i RZ(π·φ_i) RY(π·θ_i) |0⟩       # 乘积态      │
│                                                            │
│ Step 2  重上传编码（S → 角度）×(D-1) 次                      │
│   S_norm = LayerNorm(S)                                    │
│   S_scaled = γ · S_norm                    # γ 缩放输入    │
│   θ_S, φ_S = proj_S(S_scaled)  → (B, V, N, 2)            │
│   |ψ⟩ = U_ent · (⊗_i RZ(π·φ_Si) RY(π·θ_Si)) |ψ⟩         │
│                                                            │
│ Step 3  保真度核                                            │
│   K[b,i,j] = |⟨ψ_i|ψ_j⟩|²                                │
│   性质：K 对角线恒为 1；K 对称正定（Gram 矩阵）             │
│                                                            │
│ Step 4  消息传递 + 残差修正                                  │
│   H' = (1/V) · K · (W_q H)                  # GAT 风格聚合 │
│   qcc_out = LayerNorm(H + H')                              │
│   correction = proj(qcc_out)                # (B, V, d → H) │
│   y = y_main + α · correction                # α 可学      │
└────────────────────────────────────────────────────────────┘
```

**关键设计点**：
- **N=10、D=2 时特征空间 = 2^10 = 1024 维复数**，通过 BLAS/GPU 高效经典模拟（QKCS）
- **双投影**：proj_H(d=512 → 2N=20) + proj_S(2M=64 → 2N=20)，Xavier 初始化
- **可学习 γ**：S 路调制强度，init=0.5, clamp [0.1, 2]
- **首层乘积态优化**：避免逐 qubit matmul，直接构造 ⊗_i |ψ_i⟩
- **纠缠层用置换索引**：CNOT 链 = 对 1024 维态矢量的 index_select

## 6. 设计决策记录

| # | 决策 | 理由 | 替代/消融 |
|---|---|---|---|
| D1 | 角度编码（非振幅） | 振幅编码真机不可行 + 纯振幅核=线性核 | 振幅版已否 |
| D2 | 首层 H 角度 + 重上传 S 角度 | "身份+调制"分工 | 重上传用 H / 交替 H,S |
| D3 | N=10 主线（1024 空间） | 2 倍于 512 输入 | 8/9/12 消融 |
| D4 | 时间轴对齐必做 | 窗口基准伪影（时移定理） | — |
| D5 | 频率轴对齐必做 | 周期刻度差异（尺度定理） | — |
| D6 | 幅度归一化进消融 | 规模信息 H 已有 | 有/无 |
| D7 | M=32 主线 | L 无关（物理量+采样定理） | 16/32/48 |
| D8 | 采样区间 [0,2] 主线 | 主峰两侧都有结构 | [0,1] |
| D9 | 均匀采样（非取峰） | 谱形指纹 vs 周期检测 | 对数频率轴 |
| D10 | 对齐全 detach | 确定性→不变性可证明 | 可学习软对齐 |

## 7. 消融实验矩阵

| 优先级 | 组 | 配置 | 验证什么 |
|---|---|---|---|
| **P0** | **实现** | 频谱模块 + 双阶段编码（d=512, N=10），全部消融做成 config 开关 | 每个消融 = 改一行配置 |
| **P1** | **编码消融** | 仅H / 仅S / 双阶段 | 时频各自贡献 |
| **P1** | **重上传消融** | 重上传 H 角度 vs S 角度 vs 交替 | S 调制的贡献 |
| **P2** | **跨数据集异质度 vs 增益** | 数据集按 δ̂ std % 主导周期排序，有/无对齐跑 ΔMSE | 对齐收益随异质强度单调上升 |
| **P3** | 对齐消融 | 无对齐 / 仅时间轴 / 仅频率轴 / 双轴 | 每层对齐贡献 |
| **P3** | qubit 消融 | N=8/9/10/12 | qubit 数影响 |
| **P3** | M 消融 | M=16/32/48 | 采样密度 |
| **P3** | 幅度归一化 | 有/无 | 规模信息取舍 |
| **P3** | 主线 E2 | 12 数据集，L=H=96/192/336/720（ILI 24/36/48/60） | 与 SOTA 对比 |

## 8. 当前状态

### ✅ 已完成
- DualAE-QCC 核心模块重构（qcc/spectrum.py, qcc/feature_map.py, qcc/qcc_block.py）
- d_model 从 128 修正为 512（TSL 合规）
- N 从 8 修正为 10（1024 维特征空间）
- 数据模块支持 12 个数据集（含 ILI 周频特判、ETT 12mo:4mo:4mo split）
- 训练引擎支持 AMP + 梯度累积 + 投影层权重衰减豁免
- 冒烟测试（tests/test_smoke.py）
- 6 个消融配置文件
- 旧文件清理完成

### ⏳ 待执行
- 冒烟测试验证（服务器上）
- 主线实验（dual_phase1.yaml）
- 消融实验（6 个配置）
- 跨数据集异质度 vs 增益散点图（生死线实验）

## 9. 与 TSL 协议的对齐

| 维度 | TSL 标准 | DualAE-QCC 当前 | 状态 |
|------|----------|-----------------|:----:|
| d_model | 512 | 512 | ✅ |
| Split（ETT） | 12mo:4mo:4mo | 12mo:4mo:4mo | ✅ |
| Split（其他） | 7:1:2 | 7:1:2 | ✅ |
| MSE / MAE | 必报 | 必报 | ✅ |
| 标准化 | per-channel StandardScaler | RevIN（per-window） | ⚠️ 双轨制 |
| Forward 接口 | `(x_enc, x_mark_enc, x_dec, x_mark_dec)` | `(x, x_mark, return_norm)` | ⚠️ 不兼容 |
| 同表 SOTA | 必要 | 待跑 | ❌ |

## 10. 理论卖点

1. **正定免费**：K 是 Gram 矩阵
2. **时移/尺度不变**：对齐是商映射（定理：π(g·x)=π(x) ⇒ 核群不变）
3. **传递性**：对齐到共同规范形
4. **表示冗余约简**：O(|G|)→O(1)
5. **L 无关**：M≤L/2+1 固定采样
6. **调制核不可分解**：重上传由 S 驱动时 K≠K_H·K_S
7. **可解释性**：对齐参数有物理意义，K 是显式变量关系图

## 11. 诚实边界

1. 频域增益是"旁路的旁路"（间接链路）——增益可能小
2. 对齐价值数据集依赖——同步传感器场景无差异可消
3. 对齐操作本身不新——novelty 在"位置+组合+理论"
4. 真机：10 qubit × D 层线路深度超 NISQ 可靠性
5. S 角度过大可能淹没 H——可学习投影自适

---

*相关文档*：[README.md](file:///d:/download/qutest/qcc_mamba/README.md) · [IDEA_DualAE_QCC.md](file:///d:/download/qutest/qcc_mamba/IDEA_DualAE_QCC.md) · [TSL_STANDARDS.md](file:///d:/download/qutest/qcc_mamba/TSL_STANDARDS.md) · [CHANGELOG.md](file:///d:/download/qutest/qcc_mamba/CHANGELOG.md)
