# DualAE-QCC：时频双阶段对齐量子编码（最终设计 v3）

> 最后更新：2026-08-07（v3.1：数据集与实验计划定案）
> 投稿目标：IEEE TKDE (CCF-A)
> 基础项目：QCC-Mamba（S-Mamba backbone + 量子核旁路）

---

## 0.1 v3.1 更新（2026-08-07）

**① 数据集定案（12 个，全部多变量，前提验证 v3 重跑）**：
- **前提成立（8）**：ChinaAQI(342)、PEMS_BAY(325)、ECL(321)、ILI(317)、METR_LA(207)、Weather(21)、ETTh1(7)、ETTh2(7)；
- **前提弱（4，对照组）**：Traffic(862)、Exchange(8)、ETTm1(7)、ETTm2(7)；
- 新增：ChinaAQI / METR_LA / PEMS_BAY（大变量 + 交通速度类，前提均成立——**交通用速度数据异质成立，不再一概而论**）。

**② 初步实验（先跑 6 个，快出结果）**：**ECL(321)、ChinaAQI(342)、METR_LA(207)、PEMS_BAY(325) + ETTh1(7)、Weather(21)**（4 多变量 + 2 少变量参照）。其余 6 个后续补全；个别跑不好的数据集可剔除（以实验为准）。

**③ ⚠️ 旧实验（E1/E2）结果全部作废**：d=128→512、N=8→10、编码架构改为双阶段后，**旧参数下的一切实验（含 E1/E2 数字）不再作为任何基线**。仅保留一条 motivation 结论："量子旁路在旧架构中验证过有效性"。本文所有配置（仅H/仅S/双阶段、N=10、d=512）**全部用新代码重新跑**，"仅H"= 新架构下关掉 S 路，不复用旧数据。

**④ ETTh2 定位更新**：v3 修正后 ETTh2 时移由弱变显著（δ̂ std 1.96→5.03），**"纯频率轴验证场"标签失效**——现在它是双轴都需要的主战场；数据级对齐消融对照仅剩 ILI（纯时间轴）单侧。

**⑤ 开发基线（d=512 连锁影响，实现必须遵守）**：Linear_H(512→20) 用 Xavier 初始化；H/S 拼接前各自 LayerNorm；S 路输入加可调系数 S_scaled=γ·LayerNorm(S)（γ 主线可学习 init=0.5 + 固定值 {0.1,0.5,1.0} 消融）；投影层 weight decay 豁免或减小；先沿用 backbone lr，观察 K 矩阵与 θ 尺度。

**⑥ 显存决策（2026-08-07 用户拍板）**：全模型（含梯度）约 8-12 GB，服务器 24 GB 显存足够——**不需要核矩阵分块 / gradient checkpointing / 自动降 batch**，主线直接全量计算（含 Traffic V=862），保持实现简洁。

**⑦ 旧代码决策（2026-08-07 用户拍板）**：旧部分（E1/E2 时代）代码与 configs **直接删除**，只保留新方案（12 数据集 + DualAE-QCC）；不复用旧实现、不保留 deprecated 目录。

**⑧ 其他实现规范（2026-08-07 确认）**：① δ̂ 共识时钟 = **样本内留一均值**（每样本独立、跨样本不混合、全 detach，不依赖 batch 组成）；② S 全 detach + Linear_S 有梯度 = 设计意图（backbone 不被旁路影响，旁路自己学投影）；③ 新数据集统一转 (T,V) CSV 第一列时间戳（METR_LA/PEMS_BAY 取 .h5 的 `data[:,:,0]` 速度通道，丢弃邻接矩阵）。

---

## 0. 一句话定位

在 S-Mamba 主干上并联**时频双阶段对齐量子编码旁路**：首层用 backbone 语义特征 H 编码"变量身份"，数据重上传层用**三层对齐后的频谱特征 S** 做"调制"，保真度核度量跨变量相似度作为消息传递权重。**理论身份：对时移群/尺度群的商空间表示——把 RevIN 从统计非平稳推广到结构非平稳。**

---

## 1. 最终架构

```
x (B, L, V)                                    L=输入长度，V=变量数
  → RevIN → S-Mamba Backbone → y_main + H (B,V,d)   d=512（合规后）
  → 【频谱模块】rfft → 三层对齐 → 重采样 M 点 → S (B,V,2M)   [全 detach]
  → 【量子旁路，最终方案】
       首层：|ψ⟩ = ⊗ RZ(πφ_Hi)RY(πθ_Hi)|0⟩        θ_H=Linear_H(d→2N)，N=10
       重上传：|ψ⟩ = U_ent·(⊗ RZ(πφ_Si)RY(πθ_Si))·|ψ⟩ ×(D−1)  θ_S=Linear_S(2M→2N)
       → K[i,j]=|⟨ψ_i|ψ_j⟩|² → H'=(1/V)K(W_q H) → y = y_main + α·proj(H+H')
  → RevIN denorm
```

**关键设计**：
- **H 只进首层**（语义身份），**S 每层重上传**（频谱调制）——"身份固定 + 调制变化"；
- 酉门复合不覆盖信息：R(θ_S)·R(θ_H)|0⟩ 是复合旋转，最终态同时依赖 H 和 S；
- qubit 主线 **N=10**（2¹⁰=1024，正好 2 倍于 512 输入），9/8/12 消融。

---

## 2. 频谱特征构造：三层对齐（全 detach 确定性函数）

### 2.1 频谱分解
```
X(k) = Σ_{n=0}^{L-1} x(n)·e^{-i2πkn/L}     k=0..L−1     DFT
rfft: L=720 → 361 个复数值（实信号共轭对称 X(k)=conj(X(L−k))，只留一半+1）
A(k)=|X(k)|   幅度谱        φ(k)=arg X(k)   相位谱      X(k)=A_k·e^{iφ_k}
```

### 2.2 第 1 层：时间轴对齐（相位去趋势，去时移基准）
```
时移定理：x(t−δ) ⟷ X(f)·e^{-i2πfδ}      → 相位谱多一条斜率 2πδ 的直线
δ̂_v = 变量 v 相对"共识时钟"的时移（归一化互相关最优滞后，见下）
φ̃(f) = φ(f) − 2πf·δ̂_v                    （每个频率减自己的偏移量 2πf·δ̂）
```
- **含义**：相位谱 = 直线（时移指纹/环境伪影）+ 偏离（相对相位/波形形状）；减斜率 = 统一起点（标杆=斜率 0），保留形状；
- **⚠️ δ̂ 的估计方法（2026-08-06 前提验证修正）**：**不能用单变量相位谱线性回归**——该量是"相对窗口起点"的，所有变量共享同一窗口起点，不度量跨变量差异；且多成分相位谱非直线、噪声区相位随机（合成数据验证：零时移纯余弦被估成 δ̂≈59）。**正式方案：δ̂_v = 变量 v 与"其余变量留一均值（共识时钟）"的归一化互相关最优滞后**（标准信号处理做法，稳健）；实现上在 batch 内对每条序列与该 batch 其余变量均值做互相关，取峰值滞后为 δ̂_v（detach，不参与梯度）。

### 2.3 第 2 层：频率轴对齐（主频归一化，去周期刻度差异）
```
尺度定理：x(αt) ⟷ (1/|α|)·X(f/α)     （时间缩放 α = 频率轴缩放 1/α + 幅度缩放 1/|α|）
f̂_peak = argmax A(f)（幅度谱主峰）
f̃ = f / f̂_peak                          → 主峰搬到 f̃=1.0（坐标，不是"第 1 个采样点"）
采样区间 [0,2]（主线，主峰两侧）/[0,1]（消融）
重采样：在 f̃ 轴上取 M=32 个均匀点，映射回原始格点（多为小数）→ 线性插值
```
- **L 无关**：频率是物理量、L 只决定格点密度（分辨率=fs/L），不决定谱形；M=32 ≤ L/2+1（L=96→49≥32）；S 恒 64 维，一个旁路服务所有 L（96/192/336/720）；
- **为什么均匀采样不是取峰**：取峰=周期检测（保留绝对频率位置，与对齐哲学冲突、丢峰形态/峰数、噪声敏感）；均匀采样=谱形指纹（与"结构相似度"一致）。

### 2.4 第 3 层：幅度归一化（消融档，主线不做）
```
Ã = A / A_max        （每条变量独立）
```
- **决策理由（用户判断采纳）**：规模差异可能有用（大变电站 vs 小变电站）；H 已含规模语义，S 再表示可能冗余；做成消融（有/无），实验决定。尺度定理效应②。

### 2.5 输出
```
S_v = [Ã(32 幅度) ; φ̃(32 相位)] ∈ R^64    频率轴隐含在索引顺序里
```

---

## 3. 全部数学公式表

| # | 公式 | 含义 | 用在哪 |
|---|---|---|---|
| 1 | e^{iθ}=cosθ+i·sinθ | 欧拉公式（复数=旋转） | FFT 的数学基础 |
| 2 | X(k)=Σx(n)e^{-i2πkn/L} | DFT：分解成频率清单 | rfft 实现 |
| 3 | x(n)=(1/L)ΣX(k)e^{i2πkn/L} | IDFT：合成回时域 | 证明无损（双射） |
| 4 | X(k)=A_k·e^{iφ_k} | 复数极坐标 | 幅度/相位谱 |
| 5 | X(k)=conj(X(L−k)) | 实信号共轭对称 | rfft 只留 L/2+1 |
| 6 | x(t−δ)⟷X(f)e^{-i2πfδ} | 时移定理 | 时间轴对齐 |
| 7 | x(αt)⟷(1/\|α\|)X(f/α) | 尺度定理 | 频率轴/幅度归一化 |
| 8 | Σ\|x(n)\|²=(1/L)Σ\|X(k)\|² | 帕塞瓦尔（能量守恒） | 幅度谱物理意义 |
| 9 | x∗g⟷X·G | 卷积定理 | FEDformer 原理（related work） |
| 10 | R_XX(τ)⟷\|X(f)\|² | 维纳-辛钦 | Autoformer 原理（related work） |
| 11 | φ̃=φ−2πf·δ̂_v | 相位去趋势 | 时间轴对齐 |
| 12 | δ̂_v = 互相关最优滞后（vs 共识时钟） | 时移量估计 | 时间轴对齐 |
| 13 | f̃=f/f̂_peak | 主频归一化 | 频率轴对齐 |
| 14 | Ã=A/A_max | 幅度归一化 | 消融档 |
| 15 | Ã[m]=interp(A, f̃_m·f̂_peak) | 重采样 | M=32 采样 |
| 16 | S=[Ã;φ̃]∈R^{2M} | 谱形指纹 | 旁路输入 |
| 17 | \|q_i⟩=RZ(πφ_i)RY(πθ_i)\|0⟩ | 角度编码 | 量子编码 |
| 18 | \|ψ⟩=\|q_1⟩⊗...⊗\|q_N⟩ | 张量积组合 | 2^N 维态 |
| 19 | K[i,j]=\|⟨ψ_i\|ψ_j⟩\|² | 保真度核 | 核矩阵（Gram 正定） |
| 20 | H'=(1/V)K(W_q H) | 平均聚合消息传递 | 旁路修正 |

---

## 4. 设计决策记录（每条含理由与替代）

| # | 决策 | 理由 | 替代/消融 |
|---|---|---|---|
| D1 | 角度编码（非振幅） | 振幅编码真机不可行（任意态制备需指数门）+纯振幅核=线性核 cosine² | 振幅版已否 |
| D2 | 首层 H 角度 + 重上传 S 角度 | "身份+调制"分工；S 对齐→核自动不变；实现改一行 | 重上传用 H（=原QCC）/交替 H,S |
| D3 | N=10 主线（1024 空间） | 2 倍于 512 输入，满足"空间≥输入"直觉，开销可控 | 8/9/12 消融 |
| D4 | 时间轴对齐必做 | 窗口基准伪影（时移定理保证线性项） | — |
| D5 | 频率轴对齐必做 | 周期刻度差异（尺度定理） | — |
| D6 | 幅度归一化进消融 | 规模信息 H 已有，S 重复表示可能冗余 | 有/无 |
| D7 | M=32 主线 | L 无关（物理量+采样定理）、统一坐标 | 16/32/48（64 仅 L≥126） |
| D8 | 采样区间 [0,2] 主线 | 主峰两侧都有结构（低频+谐波） | [0,1] |
| D9 | 均匀采样（非取峰） | 谱形指纹 vs 周期检测；取峰与对齐哲学冲突 | 对数频率轴采样 |
| D10 | 对齐全 detach | 确定性→不变性可证明；与"电路无可学参数"风格一致 | 可学习软对齐（未来） |
| D11 | 不用原 QCC 复用作约束 | 用户明确：新架构不迁就旧实验 | — |

---

## 5. 消融实验矩阵（v3.1：优先级 P0–P3）

| 优先级 | 组 | 配置 | 验证什么 |
|---|---|---|---|
| **P0** | **实现（一切前提）** | 频谱模块 + 双阶段编码（d=512, N=10），**全部消融做成 config 开关** | 每个消融 = 改一行配置 |
| **P1** | **编码消融**（ECL 先跑） | 仅H（=新架构关 S 路）/仅S/双阶段——**全部新参数新跑，不复用 E1/E2** | 时频各自贡献 |
| **P1** | **重上传消融**（ECL 先跑） | 重上传 H 角度 vs S 角度 vs 交替 | S 调制的贡献 |
| **P2** | **跨数据集异质度 vs 增益（生死线）** | 数据集按 δ̂ std % 主导周期排序，有/无对齐跑 ΔMSE，画散点图 | 对齐收益随异质强度单调上升——最强机制证据 |
| **P3** | 对齐消融 | 无对齐/仅时间轴/仅频率轴/双轴(+幅度) | 每层对齐贡献 |
| **P3** | qubit 消融 | N=8/9/10/12 | qubit 数影响 |
| **P3** | M 消融 | M=16/32/48（+对数轴） | 采样密度/坐标 |
| **P3** | 幅度归一化 | 有/无 | 规模信息取舍 |
| **P3** | 频点数/采样区间 | [0,1] vs [0,2] | 谱形覆盖范围 |
| **P3** | 主线 E2 | **12 数据集（8 主战场 + 4 对照组）**，L=H=96/192/336/720（ILI 24/36/48/60） | 与 S-Mamba/iTransformer/PatchTST/DLinear/TimesNet 对比 |

> **前提验证（✅ 已完成，v3 重跑 12 数据集）**：8 成立（ChinaAQI/PEMS_BAY/ECL/ILI/METR_LA/Weather/ETTh1/ETTh2）、4 弱（Traffic/Exchange/ETTm1/ETTm2）。
> **初步实验 6 个**：ECL、ChinaAQI、METR_LA、PEMS_BAY（多变量）+ ETTh1、Weather（少变量参照）；其余补全，跑不好可剔除。

---

## 6. 文献对照（related work 素材）

| 模型 | 频域用法 | 与 L 关系 | 对齐 |
|---|---|---|---|
| Autoformer | 自相关（功率谱逆变换，维纳-辛钦） | 绑定 | 无 |
| FEDformer | 频域注意力，固定 mode 数 | mode 固定/位置随 L | 无 |
| TimesNet | FFT 找 top-k 周期 reshape 2D | 绑定 | 无（只找周期） |
| FreTS | 频域 MLP（全 bin） | 绑定 | 无 |
| FiLM | 固定 Legendre 基函数 | 与 L 无关 | 无 |
| **本文** | **对齐 + 固定 M 采样 + 量子核** | **与 L 无关** | **三层** |

**差异化**："对齐（不变性）+ 固定采样（L 无关）+ 量子核（显式非线性）"三合一，文献空白。
**诚实 lineage**：对齐框架来自 HAQJSK（共同原型→传递→正定）；频率规整来自 VTLN/对数轴（信号处理传统）；固定采样来自 FEDformer/FiLM。related work 必须主动写清，否则审稿人拿 VTLN 打。

---

## 7. 理论卖点

1. **正定免费**：K 是 Gram 矩阵；
2. **时移/尺度不变**：对齐是商映射（定理：π(g·x)=π(x) ⇒ 核群不变）；
3. **传递性**：对齐到共同规范形（与 HAQJSK"共同原型→传递→正定"平行）；
4. **表示冗余约简**：O(|G|)→O(1)（等价类坍缩）；
5. **L 无关**：M≤L/2+1 固定采样（采样定理）；
6. **调制核不可分解**：重上传由 S 驱动时 K≠K_H·K_S（数据相关酉演化）；
7. **可解释性**：对齐参数（δ̂_v、f̂_peak）有物理意义，K 是显式变量关系图（黑盒→白盒）。

---

## 8. 诚实边界与风险

1. **频域增益是"旁路的旁路"**（间接链路：对齐→核准→消息准→预测好）——增益可能小，**"跨数据集异质度 vs 增益"实验是生死线**；
2. **前提验证已完成（15 数据集，8/15 成立）**：对齐价值**数据集依赖**——同步传感器场景（Traffic/PEMS/Solar）无差异可消（前提弱，对齐预期 ΔMSE≈0）；异质场景（ECL/SMD/Weather/ILI）前提成立。**论文必须诚实声明适用场景 = "异质多变量耦合建模"**，并将弱前提数据集作为对照组（对齐不损性能本身就是卖点）；
3. **对齐操作本身不新**（≈linear phase removal / VTLN）——novelty 在"位置（核内）+组合（量子×对齐）+理论（不变性）"；
4. **真机**：角度编码可真机，但 10 qubit × D 层线路深度超 NISQ 可靠性——定位经典模拟（QKCS）+真机未来工作；
5. **S 角度过大可能淹没 H**——可学习投影自适 + 消融验证；
6. **SMD/ILI 的 TSL 协议兼容性**需确认（SMD 格式特殊、ILI 周频），E2 表中按 TSL 标准处理。

---

## 9. 待办

### P0：实现（开发中）
- [ ] 频谱模块（rfft → 三层对齐[互相关 δ̂ / 主频归一化 / 幅度消融档] → grid_sample M=32 → S (B,V,2M)），全 detach
- [ ] 双阶段编码（首层 θ_H=Linear_H(512→2N)、重上传 θ_S=Linear_S(2M→2N)，N=10）
- [ ] **全部消融做成 config 开关**（S 路开关 / 重上传来源 / 对齐层开关 / N / M / γ / 幅度归一化 / 采样区间）
- [ ] d=512 初始化规范：Xavier、H/S 各自 LayerNorm、S 路 γ 系数、投影层 weight decay 豁免

### P1：ECL 单数据集快速验证
- [ ] 冒烟测试（K 矩阵合理、θ 尺度正常）
- [ ] 编码消融（仅H/仅S/双阶段，全部新参数，不复用 E1/E2）
- [ ] 重上传消融（H vs S vs 交替）

### P2：生死线机制实验
- [ ] **跨数据集异质度 vs 增益**（12 数据集排序，有/无对齐 ΔMSE 散点图）

### P3：完整化
- [ ] 对齐消融 / qubit 消融 / M 消融 / 幅度归一化 / 采样区间
- [ ] E2 全数据集 benchmark（d_model=512 合规，12 数据集，先 6 个初步再补全）

### 论文
- [ ] 引言按"缺陷驱动"重写（经典核缺频域→QCC 隐式→频域模型无对齐→本文三层）
- [ ] 理论章节：定理（商映射不变性）+ 引理（调制核不可分解）
- [ ] 可解释性四件套（对齐参数/K 热力图/频谱对比/案例）

---

*相关文档*：[PROJECT_IDEA.md](file:///d:/download/qutest/qcc_mamba/PROJECT_IDEA.md)（原 QCC）· 本文件 v2（对称性约简框架，被 v3 取代）
