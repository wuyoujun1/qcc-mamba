# 架构改造计划（ARCH_REFORM_PLAN）

> 日期：2026-08-14  
> 来源：合成 `EXPERIMENT_SCREENING.md`（怎么跑）+ `STRATEGY_BEAT_SMAMBA.md`（怎么验收）+ DeMa 论文启发（改什么）  
> 核心判断：**改核心架构才是关键，加 patch 不会有大改变**——大改小改都试，用筛选机制砍人。  
> 参考声明：借鉴 DeMa（2601.05527v2）的**架构哲学**（双路径分工、并列组织、加权融合），**不复制它的模块**（AFF 分解 / DALA 线性注意力）——我们保留并强化自己的差异化（频域双轴对齐 + 量子核显式耦合 + 理论链）。

---

## 0. 总原则

1. **先改架构，再加补丁**：温度/topk/head 是局部 patch；架构层面的信息流设计（分工、位置、输入源）才是决定性的；
2. **大改小改都试**：P0 小改（一行级）→ P1 中改（模块级）→ P2 大改（架构重构），每档 1~2 个候选进筛选；
3. **筛选/确认分离**：单 seed + 快数据集 + L=96 砍人；多 seed + 大变量 + L=720 只给赢家；
4. **差异化红线**：借鉴 DeMa 的"时间/跨变量双路径分工"哲学，但跨变量度量必须用我们的量子核 + 对齐频谱，不换线性注意力。

---

## 1. 胜负评价标准（沿用 STRATEGY §0）

### 筛选阶段（单 seed 42）

- 相对提升 = (base − ours) / base **≥ 3~5%** 才进确认；低于门槛视为噪声直接砍。

### 确认阶段（3 seed：42/2024/7）——五条全满足才宣布"更好"

| # | 判据    | 标准                                       |
| - | ----- | ---------------------------------------- |
| 1 | 胜场比例  | 全部 cell（数据集 × L）赢 ≥ 60%                  |
| 2 | 平均提升  | 跨 cell avg MSE 相对提升 ≥ 3%                 |
| 3 | 主战场必赢 | 大变量（ECL/ChinaAQI）avg 必须赢                 |
| 4 | 对照组不损 | Traffic/Exchange 弱异质 ΔMSE ≈ 0            |
| 5 | 显著性   | paired Wilcoxon p < 0.05 或 3-seed 增益方向一致 |



---

## 2. 执行策略（沿用 EXPERIMENT_SCREENING）

| 项     | 配置                                                      |
| ----- | ------------------------------------------------------- |
| 筛选数据集 | etth1（7）/ weather（21）/ **electricity（321，必须有）**         |
| 确认数据集 | 筛选集 + ChinaAQI（342）或 PEMS_BAY（325）                      |
| L 端点  | 只跑 96（砍人）+ 720（长 H 兑现），192/336 留确认                      |
| 种子    | 筛选单 seed(42)；确认 3 seed(42/2024/7)                       |
| 记录    | 统一 summary csv（id/config/ds/L/seed/status/mse/mse_norm） |

---

## 3. 架构改造候选（按成本分档）

### P0 小改（一行~几十行，先跑，不阻塞）

**P0-1 · δ̂ 时滞入 S（回应 DeMa 的 delay-aware，潜力最大）**

- 做法：`S = [Ã; φ̃] → [Ã; φ̃; δ̂]`（64→65 维），δ̂ 仍 detach
- 原因：时滞是 backbone 实测零响应（1e-6）拿不到的独有信息；DeMa 赢 S-Mamba 的核心就是时滞建模——我们目前"消除时滞"是最大软肋，保留 δ̂ 让 K 同时度量"形状相似 + 时滞关系"，一步化解

**P0-2 · 消息传递置信度加权（DeMa ρ_ab 启发）**

- 做法：softmax(K/T) 后按行做置信度缩放（如行熵惩罚 / 最大权重函数），压低"所有邻居都弱相关"的变量的消息
- 原因：DeMa 用峰值相关强度 ρ_ab 加权（强对贡献大、噪声对压低）；低成本修"核无选择性"的残余问题

**P0-3 · 经典核对照（rbf，定位实验，必做）**

- 做法：K 换 rbf(H)+S 同投影/同 T/topk/同位置
- 原因：判定"量子核是否提供增量"——同机制不同核，是 DeMa 式 Repl.（替换）消融的论文必需项

### P1 中改（模块级）

**P1-1 · QuantumMix 输入源换"专用频域特征"（降信息重合，DeMa AFF 启发）**

- 做法：K 的输入从"主干 H（压缩摘要）"改为"对齐频谱 S 本身 + 频域统计"（或 H 与 S 独立双路、S 路加重）
- 原因：现在 K 读主干 H → 与主干隐式学到的高度重合（诊断过"增量被吸收"）；DeMa 变量路径读 AFF 专用高频分量 → 信息互补。让 K 读主干拿不到的频域结构

**P1-2 · 路径级可学习融合（DeMa α/β 启发）**

- 做法：H 路径与 S 路径加显式可学习加权（α·f(H)+β·g(S)），替代/叠加输入级 γ 缩放
- 原因：DeMa 用 α/β 融合时间/变量两路径（任务自适应）；我们是输入级 γ，路径级融合是新增自由度，做成消融档

**P1-3 · head_agg + 频谱注入组合（沿用 STRATEGY ②③）**

- 做法：`注入（S 加进 embedding，早期先验）` + `head_agg（K 拼预测头，后期耦合）` 组合
- 原因：频谱注入已有 2-seed 稳定收益（etth1 96/720）；两个信息通道（一个喂主干、一个喂预测头）互不打架

### P2 大改（架构重构，DeMa 哲学 + 量子核特色）

**P2-1 · 双路径重构：主干单向化 + 量子核独占跨变量（对标 DeMa 但保留量子核）**

- 做法：
  ```
  输入 x → 频域双轴对齐（我们的 S）
    ├→ 时间路径：主干去变量维双向扫描（单向 SSM，只做时间建模）   ← 借鉴 Mamba-SSD 的"时间纯净"思想
    ├→ 变量路径：量子核独占跨变量（输入 = 对齐频谱特征，全对全 K）← 借鉴 DALA 的"全对全独占"思想
    └→ 加权融合（α·时间 + β·量子核）→ 预测头
  ```
- **和 DeMa 的差异化（不是抄袭）**：
  |       | DeMa                     | 我们                              |
  | ----- | ------------------------ | ------------------------------- |
  | 输入分解  | AFF 按幅度 top-θ 分解         | **频域双轴对齐**（相位/尺度归一化，可证明不变）      |
  | 跨变量度量 | linear attention（可学习、黑盒） | **量子核 K**（显式、可解释、Gram 正定、成分级归因） |
  | 理论    | 定理1（非正交重叠）               | 对齐不变性 + 不可分解引理 + 浓度定理           |
  | 借鉴点   | 双路径并列 + 分工纯净 + 加权融合      | ← 只借鉴组织哲学                       |
- 原因：DeMa 消融证明"显式跨变量路径"值 41% 增益；我们的 K（全对全）结构正确但被主干隐式双向扫描稀释——单向化后 K 成为**唯一**跨变量通道，分工纯净、信息互补、叙事最强

---

## 4. 决策门

```
第 1 轮筛选（本周）：P0-1 + P0-2 + P1-1（改输入源）共 3~4 个候选
                     × {etth1, weather, electricity} × L=96 × 单 seed
    ├─ 有候选 ≥3% 优于 plain → 第 2 轮：加 P1-2/P1-3 + 3 seed 确认（§1 五条判据）
    ├─ 全部 <3% → 直接评估 P2-1 双路径重构（DeMa 哲学背书，值得一搏）
    └─ P0-3 经典核对照随时跑（定位实验，不阻塞方向）
```

## 5. 一句话

**先改架构（P0 输入源/时滞 → P1 融合/组合 → P2 双路径重构），再用筛选机制砍人，用五条判据验收——借鉴 DeMa 的"双路径分工"哲学，但跨变量度量始终用我们的量子核 + 对齐频谱，不换线性注意力。**
