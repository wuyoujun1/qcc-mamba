# 经典—量子统一编码接口（PAI）：从 SSM 隐状态 (h, s) 到 5-qubit 量子态 φ

> 理论章节（只做理论，不做实验）。配套阅读：`METHOD_ARCHITECTURE.md`（架构）、`EXPERIMENT_CHAPTER.md`（结果）、`qcc/feature_map.py`（现编码）、`qcc/quantum_mix.py`（现混合层）、`qcc/kernel.py`（现核）。
> 版本 v3（2026-08-26）：公式全部改为纯文本记号（无 LaTeX）。

---

## 0. 一句话

把 Mamba 主干隐状态 **h（内容）** 与对齐频谱 **s（上下文/调制）** 统一编码为一个 5-qubit 量子态：

    φ(h,s) = Σ_b √p_b(h) · e^(i·θ_b(s)) · |b>

其中 **h 走振幅通道**（p(h)=softmax(W_r·h)，内容决定选中哪些基态），**s 走相位通道**（θ(s)=W_θ·s，上下文决定基态间相对相位）。映射自动归一、闭式、光滑；诱导量子核因子化为：

    K_ij = B(h_i,h_j)² · |M_ij|²

经典内容相似度 B² × 谱相位相干 |M|²——后者携带反对称（有向）干涉项，是经典实核（rbf）在多项式成本下拿不到的结构。

---

## 1. 动机与文献定位

### 1.1 三篇量子启发论文提炼的"经典→量子过渡"三件套

从本批次三篇论文（MQN/ESWA-2026、QPFE-ERNIE/TCYB-2024、ICWE-QNN/CICWE-QNN/TKDE-2023）逐字精读提炼，它们共用一台机器：

1. **复振幅分解**：z_j = r_j · e^(i·θ_j)。振幅 r_j 装"是什么"（内容/语义），相位 θ_j 装"在什么语境下"（情感/位置/说话人/词序/多义）。
   - ICWE: |φ> = Σ_j r_j·e^(i·c_j)·|e_j>
   - QPFE: |t> = Σ_i r_i·e^(i·θ_i)·|e_i>
   - MQN: |u> = Σ_l a_l·e^(i·φ_l)·|k_l>
2. **相位是加法通道、振幅是乘法通道**：上下文信息以求和进入相位。
   - MQN 的 CFE：φ_j = pos·w_p_j + spe·w_s_j + θ_j（位置×可学习频率 + 说话人×可学习频率 + 初始相位）
   - QPFE 词序：e^(i·θ_order) · r · e^(i·θ) = r·(cos(θ+θ_order) + i·sin(θ+θ_order))——序信息直接加进相位
3. **测量**：p_i = |<λ_i|φ>|²（或 p_i = Tr(ρ·O_i)）

**推论**：经典实向量是相位归零（θ=0）的特例——复平面本身就是"经典→量子"的连续路径。

### 1.2 我们对齐到 QCC-Mamba

- h（主干隐状态）是"内容"→ 进振幅；s（对齐频谱）是"上下文/频域调制"→ 进相位。5 qubit → ℂ³²。
- **三层相位链**（代码 qcc/spectrum.py 的构造带来的自然延伸）：

    时滞 δ̂ ──傅里叶──> 频域相位 φ_freq ∈ S ──PAI──> 量子相位 θ(s)=W_θ·s ──核──> Im<φ_i|φ_j> 有向 lead-lag 耦合

  其中 S = [幅度谱 A; 相位谱 φ_freq]（代码如此构造）。时滞方向信息一路传到核的反对称项。

### 1.3 文献锚点（本理论必须对齐的三个已知结果）

1. **相位编码 = 复傅里叶特征**：e^(i·θ(s))（θ 线性）正是复数傅里叶特征；RFF 的 cos(ω·x+b)（Rahimi & Recht，Bochner 定理）是其实数形式，代码 qcc/classical_kernels.py 已有 rff_kernel 经典对照。→ 相位通道 = 可学习频率的复傅里叶特征映射。
2. **纯相位核已有专名（SPE）**：Spectral Phase Encoding（EPJ Quantum Technology 2026）的核 K = (1/2^(2n))·|Σ_j e^(i·Δ_j)|² 正是本理论"纯相位极限"（引理 2）。→ 纯相位形式是"正宗的"，但新颖点只能在振幅-相位耦合（B²|M|² 因子化）。
3. **复值虚部 = 编码反对称/方向结构的经典手段**：ComplEx（Trouillon et al. 2016）用复数共轭使内积非交换，从而建模反对称关系（DistMult 实值做不到）。→ Im<φ_i|φ_j> 的 lead-lag 方向通道有干净的经典先例。

### 1.4 诚实边界（§9 详述）

逐变量 5-qubit PAI 的核是可经典模拟的（32 维复内积），不构成"量子硬度"声称。正当优势定位是复值特征接口（方向通道 + 干涉选择性 + 内容加权），并把振幅编码/RFF/SPE 作为特例统一。这一边界直接呼应 Huang et al. 2021 的几何差结论与代码 kernel.py 中 PQK 注释引的"数据受限区 PQK 赢 RBF"。

---

## 2. 记号与问题设定

| 记号 | 含义 | 形状 |
|---|---|---|
| N = 5 | 每变量量子比特数 | — |
| H = ℂ^(2^N) = ℂ^32 | 5-qubit Hilbert 空间 | — |
| |b>，b ∈ {0,1}^5 | 计算基态 | 32 个 |
| V | 变量数 | — |
| h_v ∈ ℝ^d | 变量 v 的 Mamba 主干隐状态 token（"内容"，经 pre_ln） | d=256 |
| s_v ∈ ℝ^(2M) | 变量 v 的对齐频谱 S=[A;φ_freq]（确定性、全 detach，"上下文"；delay_in_s 时 2M+1） | 2M=64 |
| W_r ∈ ℝ^(32×d)，β_r ∈ ℝ^32 | 振幅通道参数 | — |
| W_θ ∈ ℝ^(32×s_dim)，β_θ ∈ ℝ^32 | 相位通道参数 | — |

**问题设定**：构造接口 E: (h_v, s_v) → φ_v ∈ H，满足 (i) ||φ_v||=1；(ii) 光滑；(iii) 经典情形平滑退化回经典编码；(iv) 闭式；(v) 可编译成 5-qubit 电路；(vi) 与现有核/混合机器无缝衔接。

---

## 3. 定义：相位—振幅接口（Phase–Amplitude Interface, PAI）

**定义 1（PAI）**。对每个变量 v：

    φ_v = Σ_{b∈{0,1}^5} √p_b(h_v) · e^(i·θ_b(s_v)) · |b>        (1)

- **振幅通道（h）**：p(h_v) = softmax(W_r·h_v + β_r) ∈ 31 单纯形（32 个非负且和为 1），振幅取 √p_b ≥ 0。
- **相位通道（s）**：θ(s_v) = W_θ·s_v + β_θ ∈ ℝ^32（W_θ 的列 = 可学习频率，承接 MQN 的 w_p/w_s 到连续特征）。

自动归一：||φ_v||² = Σ_b p_b(h_v) = 1。✓
复振幅 z_b = √p_b·e^(i·θ_b) = r_b·e^(i·θ_b)，正是三篇论文的形式（振幅编码标准配方：振幅=概率平方根）。

**关键不对称性**：h 决定哪些计算基态被选中（几何/身份），s 决定这些基态之间的相对相位（干涉/调制），二者只在核的内积处相互作用——这是"h、s 作为一个整体"的数学实现。

---

## 4. 平滑过渡：经典极限与 λ-形变族

**引理 1（经典极限，相位归零）**。β_θ=0 且 s_v=0 时，φ_v = Σ_b √p_b(h_v)·|b> 是实数（零相位）态 = h 的纯振幅编码，诱导核为：

    K_ij = (Σ_b √(p_b(h_i)·p_b(h_j)))² = B(h_i,h_j)²

即 p(h_i)、p(h_j) 的 Bhattacharyya 系数平方——经典、可解释的核。PAI 包含代码现有 amplitude_encoding 分支作为 s=0 纤维；PAI 是它的复化。

**引理 2（纯相位极限，内容均匀）**。p(h_v) = 均匀（1/32）时：

    φ_v = (1/√32) · Σ_b e^(i·θ_b(s_v))·|b>
    K_ij = (1/32²) · |Σ_b e^(i·(θ_b(i)−θ_b(j)))|²

即文献中的 Spectral Phase Encoding 核（EPJ QT 2026）。PAI 包含 S-only 赢家配置作为"振幅均匀化"纤维。

**定理 1（λ-形变：经典→量子的一族光滑过渡）**。单参数族（λ ∈ [0,1]）：

    φ_v(λ) = Σ_b √p_b(h_v) · e^(i·λ·θ_b(s_v)) · |b>        (2)

(i) φ_v(0) 是 h 的经典振幅编码，φ_v(1) = φ_v 是完整接口；
(ii) λ → φ_v(λ) 在 [0,1] 上 C∞ 光滑，且 ||dφ/dλ|| ≤ ||θ(s_v)||_∞；
(iii) 诱导核 λ → K_ij(λ) 关于 λ 实解析，任何 λ=0 的经典核可被绝热形变到 λ=1 的量子核。

> **λ 的身份（代码定）**：λ 是理论形变拨盘，不是训练参数——训练取 λ=1（全相位），消融 A2 沿 λ∈{0,0.25,0.5,0.75,1} 画平滑退化曲线实证本定理。现有可学习调制旋钮已有（theta_S_scale0 → γ = softplus → clamp[0.1,2]，控 S 强度），再加可学习 λ 是冗余。

**命题（Lipschitz 连续）**。||φ(h,s) − φ(h',s')|| ≤ L_h·||h−h'|| + L_s·||s−s'||，其中 L_s ≤ ||W_θ||（算子范数），L_h 在训练作用的紧致域上有限（LayerNorm/角度 clamp 保证），量级 ~ ||W_r|| · sup_b(1/(2√p_b))。即输入小扰动只引起量子态小扰动——端到端梯度稳定性的理论保证。

---

## 5. 统一表征：h·s 整体 vs 量子核

**定理 2（核闭式）**。

    K_ij = |Σ_b √(p_b(h_i)·p_b(h_j)) · e^(i·(θ_b(s_i)−θ_b(s_j)))|²      (3)

记 ω_b = √(p_b(h_i)·p_b(h_j)) ≥ 0（和 = B，Bhattacharyya 系数），Δθ_b = θ_b(s_i) − θ_b(s_j)，则

    <φ_i|φ_j> = Σ_b ω_b·e^(i·Δθ_b) = C_ij + i·S_ij
    C_ij = Σ_b ω_b·cos(Δθ_b)        （内容相似度加权、谱相位一致的余弦项）
    S_ij = Σ_b ω_b·sin(Δθ_b)        （谱相位对齐项，反对称）
    K_ij = C_ij² + S_ij²

- S_ij 反对称（S_ij = −S_ji），仅当两变量频谱相位差有系统结构时非零；任何实值经典核（rbf）恒实对称，无法产生此项（ComplEx 先例）。
- 对角：p_i=p_j、θ_i=θ_j ⇒ <φ|φ> = Σ_b p_b = 1，K_ii = 1。✓

**定理 3（核因子化 = h 项 × s 项）**。设 B = B(h_i,h_j)，权重 μ_b = √(p_i·p_j)/B（概率测度），定义谱相位相干：

    M_ij = Σ_b μ_b · e^(i·Δθ_b)

则：

    K_ij = B(h_i,h_j)² · |M_ij|²                                (4)

性质：
1. |M_ij| ≤ 1（三角不等式），故 K_ij ≤ B²——相位通道只能抑制内容相似度，抑制量 = 频谱相位的不相干程度。相位对准 ⇒ |M|→1 ⇒ K 保持满相似度；相位错开 ⇒ |M|→0 ⇒ K→0（破坏性干涉/去相干）。
2. 纯全局相位差不改变核（Born 规则）；核只敏感于相位差的结构。
3. 对接实验规律：强/中耦合数据集（ETTm2/ETTh1）耦合变量共享频谱相位结构 ⇒ |M|≈1 ⇒ 核保留跨变量相似度 → 消息传递高效；弱耦合/独立表计（ECL/traffic）谱相位互相独立 ⇒ |M| 被稀释 ⇒ 注入自然衰减——理论解释"量子核在耦合数据集赢、在独立数据集是税"的 38 格实证。

**命题（Bures 几何 ↔ 赢家核 quantum_exp）**。

    d_B(φ_i,φ_j) = arccos(√K_ij)

是 (h,s) 乘积空间上的联合度量（d_B=0 当且仅当 h_i=h_j 且 s_i、s_j 相位轮廓模 2π 一致）。现有赢家核 K_geo = exp(−κ·d_B²) 在 PAI 下直接变为该几何上的径向核——接口无需改动任何核代码即可无缝接入 kernel_fn='quantum_exp' 及其可学习带宽 κ。

---

## 6. 5-qubit 电路实现

**命题（可分离特例：乘积态电路，O(N) 门）**。若振幅为逐比特 Bernoulli 乘积、相位逐比特线性：

    p(b) = ∏_q a_q^(b_q)·(1−a_q)^(1−b_q)      a_q = σ(W_c·h)_q ∈ (0,1)
    θ(b) = Σ_q b_q·ω_q                        ω = W_ω·s ∈ ℝ^5

则 (1) 精确分解为单级乘积态：

    φ_v = ⊗_{q=1}^5 ( √(1−a_q)·|0> + √a_q·e^(i·ω_q)·|1> )

等价电路（门序必须 RZ∘RY：先 RY 造振幅、再 RZ 给 |1> 挂相对相位 e^(i·ω)；与现有 _rz_ry_matrix 的 RZ(φ)∘RY(θ) 约定一致）：

    φ_v = (⊗_{q=1}^5 RZ(ω_q)·RY(α_q)) · |0>^⊗5 ，  α_q = 2·arccos(√(1−a_q))

这是单比特直觉"φ ~ e^(i·θ)"的 5-qubit 多比特推广：每个比特的相位因子 e^(i·ω_q) 对应"φ 的相位"。

**命题（通用形式）**。对任意 p = softmax(W_r·h + β_r)（非因子化的 32 维分布），√p 的振幅编码可用标准 5-qubit 振幅编码电路实现（≤ 2^(N+1)−2 = 62 个受控旋转），相位通道为对角算子 D_θ = Σ_b e^(i·θ_b)·|b><b|（线性 θ 时退化为逐比特 RZ）。实现上更简单：直接构造复向量（见 §8），电路形式是理论等价物。

---

## 7. 对称性与可解释性

1. **全局相位不变**：φ → e^(i·γ)·φ（θ_b → θ_b + γ 全 b）不改变核（Born 规则）；相对相位 = 物理内容。
2. **有向（lead-lag）**：Im<φ_i|φ_j> = S_ij 在 i↔j 下反号，携带"哪个变量在主导频带领先"的方向信息——qcc/kernel.py 的 quantum_phase_geodesic_kernel 正是利用这一项（经典 rbf 无法表达）。
3. **可解释性**：p(h_v) = SSM 状态选中的"内容原子"分布；θ(s_v) = 每个原子的频谱相位；核 = 内容相似度 × 谱相位相干，每一步有物理解读。

---

## 8. 与现有代码的对照（drop-in，非重写）

**实现映射**（替换 qcc/quantum_mix.py 中 `psi = self.fmap(H_in, S_scaled)` 一处即可）：

```python
# PAI 编码（替代两阶段 EntanglingFeatureMap）
class PAIEncoding(nn.Module):
    def __init__(self, d, s_dim, n_qubits):
        super().__init__()
        self.W_r = nn.Linear(d, 2**n_qubits, bias=True)        # h → 32 概率
        self.W_θ = nn.Linear(s_dim, 2**n_qubits, bias=True)    # s → 32 相位
        # 可选：复用 EntanglingFeatureMap 的 ent_perm CNOT 置换

    def forward(self, h, s):            # h:(B,V,d) 已 pre_ln; s:(B,V,s_dim) 已 γ·s_ln 或固定尺度
        p  = F.softmax(self.W_r(h), dim=-1)     # (B,V,32)
        θ  = self.W_θ(s)                        # (B,V,32)
        z  = torch.sqrt(p) * torch.exp(1j * θ)  # (B,V,32) 复数，||z||=1
        # 可选：z = z.index_select(-1, ent_perm)  # CNOT 纠缠层
        return z
```

- **代码一致性**：Linear(·→2^N) 模式与现有 amplitude_encoding 分支的 amp_proj 一致；H_in/S_scaled 的预处理（pre_ln/s_ln/γ/固定尺度）原样保留；下游 kernel_fn、offdiag、softmax/T、W_q 消息传递、gate 全零改动。
- **参数预算**：32·256 + 32·64 ≈ 10.2k 参数，与被替换的两投影（~6.4k）同量级，相对 d_model² = 65k 可忽略。
- **赢家兼容**：现有"h 首层 + s 重上传"两阶段被统一成单级复向量；--qmix_use_S_only 语义由"相位均匀化纤维"覆盖（§4 引理 2）。

**消融设计（论文用）**：
- **A1（相位归零）**：s=0 ⇒ 纯振幅编码（对应现有 amplitude_encoding 分支）——隔离相位通道贡献。
- **A2（λ 扫描）**：λ∈{0,0.25,0.5,0.75,1} ⇒ 平滑退化曲线，实证定理 1（离线圈，不烧 GPU）。
- **A3（内容均匀）**：p → 均匀 ⇒ 纯相位（SPE）态——对照文献核。
- **A4（经典对照）**：rbf/RFF 作用于同一 2^5 维复特征 [Re;Im]（现有公平对照口径），验证 S_ij 方向项确为 rbf 盲区、且与 RFF 的差异在"可学习频率 + 内容加权 + 复值"。

---

## 9. 诚实边界（论文措辞依据）

1. **逐变量 5-qubit PAI 的核可经典模拟**：K_ij 是 32 维复向量的内积模方，O(32) 可算。按 Huang et al. 2021 的几何差理论，vs rbf-on-同特征几何差小，不构成"量子硬度"声称。这与此前 nq2 时代"单变量态核信息≡rbf"的实证一致。
2. **正当优势 = 复值特征接口**，具体三件：方向通道（Im/lead-lag，ComplEx 先例）、干涉选择性（|M|² 只能抑制）、内容加权（√(p_i·p_j) 使 h·s 乘积耦合）。这是可验证、可证伪、不靠过度声称的定位。
3. **浓度警示**：Thanasilp et al.（Nat. Commun. 2024）证明高表达力编码引发核值指数浓度。PAI 自带"浓度旋钮"：p 均匀 ⇒ 核≈SPE 易浓度；p 近 one-hot ⇒ 硬分区无选择性；中间态最利于核选择性——softmax 温度 τ（p = softmax(W_r·h/τ)）与现有 kernel_T 是同一逻辑的两端（编码侧 + 核侧）。论文应如实报告 τ 与 T 的配合。
4. **hardness 故事（若要）**：只在跨变量联合纠缠（JEQK 风格、约化密度矩阵核）下才有经典不可模拟性；但 V·N qubit 联合态在 V=7、N=5 时不可行，且 nq=2 时已实证失败。PAI 论文不声称这条。

---

## 10. 论文可写 claim 清单

1. **闭式经典—量子接口**：φ(h,s) = √softmax(W_r·h) ⊙ e^(i·W_θ·s) 将两阶段编码统一为单个 5-qubit 复态；自动归一、光滑、可编译成 RZ∘RY 单级电路。
2. **统一特例**：接口包含振幅编码（θ=0 纤维，=现有分支）、纯相位/SPE 编码（p→均匀纤维）、复傅里叶特征（θ 线性，RFF 的复化）作为特例。
3. **核因子化**：K = B²·|M|²；|M| 带反对称有向项，经典实核无法计算——为"耦合数据集赢 rbf"提供理论解释。
4. **平滑形变族**：λ∈[0,1] 绝热路径 + Lipschitz 稳定性。
5. **无缝兼容**：产出仍是 (B,V,2^N) 复态，核层（quantum_exp、可学习 κ、offdiag、kernel_T）零改动。

---

## 11. 文献对照

| 机制 | 出处 | PAI 落点 |
|---|---|---|
| 复振幅分解 z=r·e^(iθ) | ICWE Eq.(1)；QPFE Eq.(6)；MQN Eq.(4) | z_b = √p_b·e^(i·θ_b) |
| 内容→振幅 | ICWE GRU+attention 振幅；QPFE 振幅=语义 | p(h) = softmax(W_r·h) |
| 上下文→相位（加法） | MQN φ=pos·w_p+spe·w_s+θ；QPFE 词序 e^(iθ_order) | θ(s) = W_θ·s（连续化 + 可学习频率） |
| 相位=傅里叶特征 | Rahimi & Recht（Bochner）；代码 rff_kernel | θ 线性 ⇒ 复傅里叶特征（可学习频率版） |
| 纯相位核 | SPE（EPJ QT 2026） | 引理 2 特例 |
| 虚部=反对称/方向 | ComplEx（Trouillon 2016）；quantum_phase_geodesic_kernel | S_ij 项（lead-lag） |
| 可模拟性边界 | Huang 2021 几何差；Thanasilp 2024 浓度 | §9 诚实边界 |
| 测量 p=\|<λ|·>\|² | 三篇共同 | K = |<φ_i|φ_j>|²（及测地化） |

---

*理论版本 v3（2026-08-26，纯文本公式）。下一步候选：① A2 λ-扫描离线圈（纯前向，实证定理 1）；② PAI drop-in 在 ETTh1:96 冒烟（1 seed，对照现赢家）；③ 论文写法上按 §9 边界如实措辞。*
