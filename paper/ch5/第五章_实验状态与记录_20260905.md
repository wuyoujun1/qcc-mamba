# 第五章实验 · 状态与运行记录（2026-09-05）

> 本文件用于跨会话接续。中文初稿 PDF：`paper/第五章初稿_中文_20260905.pdf`（reportlab 生成，脚本 `ch5cn_pdf.py`，改文字/表后重跑 `python ch5cn_pdf.py`）。

## 口径（已拍板，别改回去）
- 命名正文用 QCCK-M；不提 seed；主表每格最优（MAE 与 MSE 同一次运行）；消融 A1–A4 为两 seed 均值；**每行 MSE 与 MAE 各自独立：第一红、第二蓝**。
- 模型横轴、数据集纵轴的一张主表；Beijing 8 基线 MAE 已补全（来自 TSL 库结果，与对比表 MSE 对齐）。
- 耦合度 x 轴 = 整段序列、逐变量标准化、两两在 ±5% 滞后窗最优滞后最大|互相关|、全对（大变量盘抽样）平均；**正文已写推导**。
- 参考文献为编号占位 [1]–[10]，最终并入全文总表。

## 已入 PDF 的实验（结果都在，不再重跑）
| 项 | 结果位置 |
|---|---|
| 主表 9盘×4×10 模型 MSE/MAE + 红蓝 | 表 2（主表） |
| 消融 5盘×4H Full/A1–A4 | 表 5 |
| R1 灵敏度（S-Mamba 6e-7→QCCK-M 1.9e-3） | 表 3 |
| R2 K 选择（ETTh1 4.1/0.89、ETTm2 5.7/0.86、ECL 142/0.26）+ 图 3a/b/c | 表/图 |
| R3 Spearman ETTh1 0.26 / ETTm2 0.37（21 对不显著=弱支持） | 表 4 |
| P1 耦合 + A2(rbf) 9盘@96 + 散点 | 表 1、图 2 |
| kernel_T/nq/gate 受控 + 720 校验 | 表 6、图 4 |
| 效率（参数量/训练时间） | 表 7 |
| 预测可视化 ETTh1 | 图 5 |
| 数据集总览、基线复现、实现细节、参考文献 | 5.1/文末 |

## 待补 / 若以后要跑（GPU）
1. **Traffic K 热力图 ✅ 已完成**：`logs/nc_traffic_96_rt_s2024.log`，mse 0.3772；K eff≈344/862、top5≈0.20；图 3d 已入 PDF（`figs/ch5_k_Traffic.png`）。
2. 预测可视化可扩 ETTm2/Beijing（脚本 `/tmp/forecast_viz.py` 换 model_id，须 GPU，mamba 前向不支持 CPU）。
3. R3 每盘只有 21 对，天然不显著——要么接受弱支持表述，要么合并多盘再算（口径要定）。
4. CMamba/SAMBA/Affirm 三条基线无 bib 条目：正文按"近期 Mamba/Transformer 变体"分组表述、未逐条编号；需要时补进 references.bib。

## 文件索引
- 数据总表：`dataops_ws/第五章_主表消融_MSEMAE_20260904.md`（含 8 基线逐格 + 北京全基线）
- 图：`paper/figs/ch5_{winheat,coupling,coupgain,sens,pred_ETTh1,k_ETTh1,k_ETTm2,k_ECL,k_Traffic}.png`（reportlab 用 PNG）
- 机制模型（重训在盘）：`dataops_ws/checkpoints/{bsl_ETTh1_96_s2024, qf2_ETTh1_96_q_base_s2024, qf1_ETTm2_96_q_freq_s2023, nc_ECL_96_rt_s2023, nc_traffic_96_rt_s2024}*`
- 字体：`/home/youjun/cjkfont/wqy-zenhei.ttf`（apt 下载解包，中文 PDF 依赖它，别删）

## 2026-09-07 追加完成
- R1 扩到 ETTm2：S-Mamba 比值 1.6e-6，QCC 7.5e-3（off-diag ~10⁴×）；表 3 现含两盘。
- R3 合并 4 盘（ETTh1/ETTm2/ETTh2/ETTm1）84 对：ρ=0.31，p≈0.004 显著；单盘 ETTh2 -0.06、ETTm1 0.36。
- 预测可视化扩到 ETTm2（图6a）与 Beijing-AQI（图6b），加上原 ETTh1 图5。
- 新增可用模型 ckpt：sm_ETTm2_96_rt_s2024、bsl_beijing_96_s2023、q_ETTh2_96_rt_s2023、q_ETTm1_96_rt_s2023、qb_beijing_96_rt_s2023。
- 效率表 ECL/Traffic 参数量填入（约 7M/9M）。
- 注：Beijing 各基线须带 --target pm2.5 否则 loader 报 list.remove（踩过的坑）。
## 2026-09-07 下午再追加（示例不只 96）
- 敏感性扩为多设置稳健区：ETTh1 96/192 与 ETTm2 96 三条曲线（kT/nq/gate），图 4 用相对自身最优的 % 偏离；正文改为"定性稳健、非刀锋最优"。
- 长步长示例：ETTh1 720 预测图 6c（窗口 QCC 0.591 vs SM 0.648，+8.8%）；K 2e（720 步 eff≈3.9/top5 0.88，与 96 一致）。
- 修正口径：主表 S-Mamba 整列改用官方复现值（exchange720/traffic336 平局）；正文去"纯 S-Mamba"；K 图注/正文说明展示的是行归一化权重（保真度核本身对称对角 1）。
- 踩坑：run_slow 取 --model_id 第一次出现，命令里只能有一个 model_id（否则全撞名丢结果）。
## 2026-09-08 按外审 GPT 建议修订（口径规则：主表/消融已有=直接用，不看 seed；只补主表没有的）
- 5.1：首页与正文去掉"每格最优"，改为"超参按验证集确定"；数据集级耦合统计定位为仅第五章分析的描述统计，与逐样本 K 区分；"真正前后脚"软化。
- 5.2："从不更差"→"基本持平"；"核心共址"→"核心多变量"。
- 5.5：Full/A1–A4 定义改为"量子核+频域监督两设计、RBF 为量子核经典替代"，删"与 5.4 相互印证"。
- 图 2：小盘 K 热力图隐藏对角(轴为真实变量名)、ECL/Traffic 改非对角 ECDF、720 仅作比较；新增表 10 Top-3 强耦合对。
- 5.7：可解释性基于 K 语义，删 attention 对比；预测图定性、仅留 ETTh1 96/720。
- 3-seed 补跑已取消（主表已有即算），未新增主表数据重复。
- K96 vs K720 Spearman=0.25/p=0.27 不显著 → 图2d 只作比较、不声称一致。